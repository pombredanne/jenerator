Allow more input file formats inside pages (rst, txt, html for starters)
Automatically detect input format and use appropriate compiler
Build default skeleton site with default template
    Optionally have site title and author name auto-fill on init
Option to daemonize the build command to watch source and build automagically
